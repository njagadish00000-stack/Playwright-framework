# Unified Playwright Automation Framework - runtime dependencies.
# Install with:  pip install -r requirements.txt
# Then install browsers (needs internet):  playwright install chromium firefox webkit

pytest>=7.4
playwright>=1.40
requests>=2.31
allure-pytest>=2.13
pytest-xdist>=3.5
Flask>=3.0
python-dotenv>=1.0
jsonschema>=4.0
Markdown>=3.5
